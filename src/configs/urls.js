const baseURL = 'https://jsonplaceholder.typicode.com'
const urls = {posts: '/posts'}

export {baseURL, urls}

export const requestsLinks = {
    todos: '/todos',
    albums: '/albums',
    comments: '/comments',
    postId: ':postId',
}